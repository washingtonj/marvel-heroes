import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  bar: {
    width: "100%",
    height: 64,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between"
  }
})